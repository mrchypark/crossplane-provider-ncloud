apiVersion: ncloud.m.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
  namespace: default
spec:
  region: KR
  site: public
  credentials:
    source: Secret
    secretRef:
      name: example-creds
      namespace: default
      key: credentials
